version: "3.7"

services:
    ts_food_market:
        container_name: shinsegae
        # image: bururaru/inc_project_front:1.0.1
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        environment:
            - NODE_ENV=development
            - CHOKIDAR_USEPOLLING=true
